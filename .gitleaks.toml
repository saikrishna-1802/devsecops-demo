# Gitleaks configuration
# Tells Gitleaks what to scan and what to ignore

title = "DevSecOps Demo — Gitleaks Config"

[extend]
useDefault = true

[[allowlists]]
description = "Ignore fixed files and documentation"
paths = [
  '''src/app\.fixed\.js''',
  '''terraform/main\.fixed\.tf''',
  '''README\.md''',
]
